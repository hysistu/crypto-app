import Layout from "components/Layout";
import TicketReportContainer from "containers/TicketReportContainer";
import React from "react";

const TicketReport = () => {
  return (
    <Layout title="Ticket Report">
      <TicketReportContainer />
    </Layout>
  );
};

export default TicketReport;
